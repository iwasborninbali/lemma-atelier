# План смены 3.09.2026, 11:00 → 23:00 WITA — ателье лемм (выбор обоих агентов, 10:55)

Владелец: свободный выбор направления; рядом, читает логи; «нет на столе». Отчёт каждые 30 минут в формате ночи:
`ψ→ · время по Бали · план: docs/PLAN_2026-09-03.md`, состояние счёта, что дала нить, что дальше.

## Дорожки

**Леммы (коллега ведёт цеха 3–7, я — нужды, опознание, обобщение, выпуск).** Нити в порядке: need-001 (жёсткость: общее k,
граница на плоскостях — контрпример в A280537), need-002 (слепота первого момента: групповая форма), need-003 (условный
Гай–Келли как многочлен), need-004 (комплекс коллинеарности — опознание), need-005 (подгруппы и плотность). Каждая лемма —
`lemmas/lem-NNN.md` по шаблону, ставка в `moves/ledger.jsonl` до счёта, противник (Opus) до приёмки, гейт `gates/lint.py` зелёный.

**Счёт (ВМ saturation-alg-1, 32 ядра, живёт до ~12:25 4.09).** Страты симметрии на **A280537** (куб без четырёх компланарных):
модель CP-SAT «≤ 3 на каждой решёточной плоскости с ≥ 4 клетками» по орбитам подгрупп O_h; сначала калибровка на известном
(n=4: a(4)=28? — по `~/saturation/certs/a280537/`), потом n=9…16 против наших свидетелей и конкурсных рекордов 2016 (`~/saturation/paper/a280537_note.tex`,
таблица до n=97). Свидетель → `certs/a280537/verify_witness.py` (определители на четвёрках) у коллеги — второй счёт независим.
Ставка до счёта — в журнале при запуске.

**Упаковка ночи (между циклами):** репо `a399138-witnesses` (свидетели n=7…11, верификатор, классы, скрипт страт) + обновление
3D-заметки v1.5 + драфт a-файла для OEIS; Zenodo — по «да» владельца на каждую запись; репо `no3in-ensemble` — после брифа 12.

## Правила смены

- Мак — только лёгкое; счёт — ВМ. Письма никому. Публикация — по слову владельца.
- Число без файла — непроверенное. «Держится у части» запрещено.
- В 23:00: итог одним файлом (`docs/ИТОГ_2026-09-03.md`), ВМ не сносить (продлена).

ψ→
