# need-005 — почему страта порядка 12 даёт максимум: подгруппы O_h, совместимые с плотной укладкой

- **источник:** ночь: страта c26_ord12 (|H|=12, rot=6, inv=y) дала максимум при n = 9, 10, 11 (116, 138, 164), c08_ord4 — при 8 и 9;
  большие группы (24, 48) дают мало (80 при n=8), группа порядка 2 — 90–92. Логи `~/saturation/logs/2026-09-02_ночь/vm/n*_results.json`.
- **вычисление:** выбор страт для A280537 (куб без четырёх компланарных) — какие подгруппы гонять первыми.
- **чего не хватает:** лемма о том, какие подгруппы H ⊂ O_h совместимы с плотностью: если H содержит движение, переводящее прямую
  ℓ в себя с орбитой из ≥ 3 точек на ℓ (например, отражение с осью на ℓ плюс сдвиг?), инвариантное множество не может иметь на
  ℓ больше двух точек орбитами ⇒ верхняя оценка |S| ≤ f(H); гипотеза: максимум по стратам достигается на H, у которых нет
  «длинных орбит на прямых», и c26 — самая большая такая группа.
- **гипотеза (conj-005):** для каждой H — явная верхняя оценка через орбиты клеток на прямых длины ≥ 3; для |H| ≥ 24 она ниже
  a(n)/1.3 при n ≥ 8 (проверить по results.json: bound страты против измеренного максимума).

- **шаг 1 (3.09, opus, ставка — нарушена):** гипотеза «главный фактор — пригодные клетки (орбиты без трёх коллинеарных клеток)» мертва:
  при n = 9 во всех 33 классах орбит с тремя коллинеарными клетками нет (образы клетки под поворотами порядка 3/4 — треугольник/квадрат
  вокруг оси, под отражениями и инверсией — пары; трёх на прямой не бывает), usable = 729 у всех, сравнивать нечего. Для A280537 такой
  же вопрос закрыт lem-003 (там орбиты убивают компланарность, а не коллинеарность).
- **честное чтение ночной таблицы (n9_results.json):** c26_ord12 — ЕДИНСТВЕННЫЙ класс, решённый до OPTIMAL (85 орбит, 116); классы
  c08_ord4, c16_ord6, c17_ord6, c18_ord6 дали те же 116 за 1800 с при незакрытых потолках 159–162 — «максимум у порядка 12» может быть
  артефактом решаемости (маленькая страта закрывается, большие — нет), а не свойством класса. Следующий шаг (не сегодня — ВМ занята n=12):
  c08/c16/c17/c18 при n = 9 по 2–4 часа с 16 воркерами; если хоть один даст 117 — нужда переформулируется в «какой класс содержит
  максимум», если все останутся на 116 с закрытым потолком — тогда вопрос «почему» законен.
